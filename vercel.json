{
  "buildCommand": "pnpm install && pnpm run build",
  "installCommand": "npm install -g pnpm@latest && pnpm install",
  "framework": "nextjs",
  "functions": {
    "app/**/*.ts": {
      "runtime": "nodejs18.x"
    }
  },
  "env": {
    "NODE_VERSION": "18"
  }
} 